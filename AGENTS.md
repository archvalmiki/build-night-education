# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

After app changes, start an Expo Go-compatible tunnel and include its current iOS URL plus the Finder-accessible QR path in the handoff. Replace stale QR files.
